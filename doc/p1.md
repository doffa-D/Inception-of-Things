
## P1

# 1. SSH into Server
vagrant ssh HdagdaguS

# 2. Check eth1 IP (Alpine uses 'ip' not 'ifconfig')
ip a show eth1

# 3. Check hostname
hostname

# 4. Check K3s status
sudo rc-service k3s status

# 5. Check both nodes in cluster
sudo kubectl get nodes -o wide


<!-- Q2: Check eth1 interface IP address -->
ip a show eth1

<!-- Q4: Verify K3s is running -->
sudo rc-service k3s status

<!-- Q5: Check nodes -->
sudo kubectl get nodes -o wide

<!-- Q6: Check deployments (verify app-two has 3 replicas) -->
sudo kubectl get deployments

<!-- Q7: Check all resources -->
sudo kubectl get all

<!-- Q8: Show Ingress -->
sudo kubectl get ingress
sudo kubectl describe ingress iot-ingress